# This repository contains several files:
# One is an up-to-date CSV file of several different variables extracted from CPS Demographic data of 10 U.S.A. cities called "CPS Demographic Data Semester Project.csv".
# Another is the code that was used to retrieve this data called "Demographic Dataset Code.ipynb".
# The last is code that is ran every month that updates this dataset to keep it up-to-date called "Dataset Updater".
